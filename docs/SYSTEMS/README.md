# Systems

System-level documentation lives here. Each major runtime system should document ownership, public interfaces, dependencies, emitted/consumed events, persistence rules, debugging hooks, and tests.
