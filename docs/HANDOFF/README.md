# Handoff

This directory contains durable handoff procedures and, when useful, milestone/session handoff notes.

Minimum session handoff is always `docs/CURRENT_STATE.md` plus current branch/PR, active spec/plan, test status, blockers, unresolved decisions, and next three concrete tasks.

A developer or AI agent must never require access to an earlier chat session to resume critical work.
