# Testing

Testing documentation and commands live here.

Planned categories include schema validation, deterministic unit tests, simulation tests, save/load round trips, headless Godot smoke tests, migration tests, AI contract/evaluation fixtures, and later export/performance tests.

Exact commands must be added as soon as the first implementation/test harness is introduced.
